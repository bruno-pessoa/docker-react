sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t brunovpessoa/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true brunovpessoa/docker-react npm run test
